{
  "name": "cluvo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test --recursive",
    "build": "bun run --filter '*' build",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
